// ask the user to enter the input using prompt and assign them to variables

let name = prompt("Enter your name");
let age = prompt("Enter your age");
let course = prompt("Enter your course");

console.log(`Student ${name} is ${age} years old and is studying ${course}`);